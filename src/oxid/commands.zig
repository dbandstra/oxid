pub const MenuCommand = enum {
    left,
    right,
    up,
    down,
    escape,
    enter,
    yes,
    no,
};

pub const GameCommand = enum {
    left,
    right,
    up,
    down,
    shoot,
    toggle_draw_boxes,
    kill_all_monsters,
    escape,
};
