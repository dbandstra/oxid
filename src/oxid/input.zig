pub const MenuCommand = enum {
    Left,
    Right,
    Up,
    Down,
    Escape,
    Enter,
    Yes,
    No,
};

pub const GameCommand = enum {
    Left,
    Right,
    Up,
    Down,
    Shoot,
    ToggleGodMode,
    ToggleDrawBoxes,
    KillAllMonsters,
    Escape,
};
